# Quick Start Guide - Health Dashboard Development\n\n## 🚀 5-Minute Setup\n\n### 1. Install & Start\n```bash\ncd hello-world\nnpm install\nnpm run dev\n```\nApp opens at `http://localhost:5173/`\n\n### 2. Hot Reload Works Out of the Box\n- Edit any `.tsx`, `.ts`, or `.css` file\n- Changes appear instantly in browser\n- No manual browser refresh needed\n\n### 3. Build for Production\n```bash\nnpm run build      # Creates optimized dist/ folder\nnpm run preview    # Test production build locally\n```\n\n## 📝 Common Tasks\n\n### Add New Health Metric (5 minutes)\n\n1. **Define Interface** - Add to `HealthMetric` array in [HealthDashboard.tsx](src/components/HealthDashboard.tsx#L12):\n```typescript\nconst quickMetrics: HealthMetric[] = [\n  // ... existing metrics\n  {\n    label: 'Blood Pressure',\n    value: '120/80',\n    change: '-5 mmHg',\n    icon: <Heart className=\"w-6 h-6\" />,\n    color: 'from-purple-400 to-pink-500',\n  },\n];\n```\n\n2. **Verify Component** - Component automatically renders from array\n\n3. **Customize Colors** - Choose gradient from `tailwind.config.ts` or add new one:\n```typescript\n// In tailwind.config.ts extend section\nbackgroundImage: {\n  'bp-gradient': 'linear-gradient(135deg, #a78bfa 0%, #ec4899 100%)',\n},\n```\n\n### Modify Oura Metrics (3 minutes)\n\n**File**: [src/components/OuraMetricsCard.tsx](src/components/OuraMetricsCard.tsx#L10)\n\n```typescript\nconst metrics: OuraMetric[] = [\n  {\n    label: 'Recovery Index',        // Change label\n    value: 85,                       // Update value\n    icon: <Zap className=\"w-8 h-8\" />,  // Change icon\n    color: 'from-azure-400 to-cyan-600',\n  },\n];\n```\n\n**Icons Available** (from [lucide-react](https://lucide.dev)):\n- `Heart`, `Shield`, `Activity`, `Moon`, `Zap`, `Award`, `TrendingUp`\n\n### Update Nutrition Goals (2 minutes)\n\n**File**: [src/components/NutritionCard.tsx](src/components/NutritionCard.tsx#L14)\n\n```typescript\nconst metrics: NutritionMetric[] = [\n  {\n    label: 'Calories',\n    current: 1850,     // Change current value\n    goal: 2200,        // Update goal\n    unit: 'kcal',\n    color: 'from-orange-400 to-red-500',\n    icon: <Flame className=\"w-5 h-5\" />,\n  },\n];\n```\n\n### Change Color Scheme (5 minutes)\n\n**Option 1: Use Existing Gradients**\nEdit hex colors in `src/components/` files:\n```typescript\ncolor: 'from-blue-400 to-purple-600'  // Tailwind color + opacity\n```\n\n**Option 2: Add Custom Gradient**\nUpdate [tailwind.config.ts](tailwind.config.ts#L16):\n```typescript\ntheme: {\n  extend: {\n    backgroundImage: {\n      'custom-gradient': 'linear-gradient(135deg, #YOUR_COLOR_1 0%, #YOUR_COLOR_2 100%)',\n    },\n  },\n},\n```\n\n**Option 3: Change Dark Background**\nEdit [src/index.css](src/index.css#L25):\n```css\nbody {\n  background: linear-gradient(135deg, #YOUR_HEX_1 0%, #YOUR_HEX_2 100%);\n}\n```\n\n### Adjust Animation Speed\n\n**File**: [tailwind.config.ts](tailwind.config.ts#L28)\n\n```typescript\nkeyframes: {\n  'float': {\n    '0%, 100%': { transform: 'translateY(0px)' },\n    '50%': { transform: 'translateY(-30px)' },  // Increase from -20px\n  },\n},\nanimation: {\n  'float': 'float 8s ease-in-out infinite',  // Change from 6s\n},\n```\n\n### Add Animation Delay to Component\n\n```typescript\n<GlassCard \n  animated \n  className=\"p-8\"\n  style={{ animationDelay: '0.2s' }}  // Add this\n>\n```\n\n### Disable Animations (Testing)\n\nComment out in components:\n```typescript\nclassName={`${className} animate-fade-in`}  // Remove animate-fade-in\n```\n\n## 🎨 Color Reference\n\n### Tailwind Color Scale\n```\nfrom-blue-400   (Cyan tones)\nto-blue-600     (Darker cyan)\n\nfrom-purple-400 (Lighter purple)\nto-purple-600   (Darker purple)\n\nfrom-pink-400   (Light pink)\nto-pink-600     (Deep pink)\n\nfrom-red-400    (Red)\nto-red-600      (Dark red)\n\nfrom-green-400  (Light green)\nto-green-600    (Dark green)\n\nfrom-yellow-400 (Yellow)\nto-yellow-600   (Dark yellow/gold)\n\nfrom-orange-400 (Orange)\nto-orange-600   (Dark orange)\n```\n\n## 🔧 Component API Reference\n\n### GlassCard\n**Props**:\n- `children: ReactNode` - Card content\n- `className?: string` - Additional Tailwind classes\n- `animated?: boolean` - Enable fade-in animation\n- `gradient?: 'primary' | 'secondary' | 'accent' | 'none'` - Overlay gradient\n- `style?: React.CSSProperties` - Inline styles (e.g., animationDelay)\n\n**Example**:\n```typescript\n<GlassCard \n  animated \n  className=\"p-6 col-span-2\"\n  gradient=\"primary\"\n  style={{ animationDelay: '0.1s' }}\n>\n  <h2>My Content</h2>\n</GlassCard>\n```\n\n### Tailwind Classes Cheat Sheet\n\n| Purpose | Classes |\n|---------|----------|\n| Padding | `p-4`, `p-6`, `p-8` |\n| Grid | `grid grid-cols-3 gap-4` |\n| Text | `text-white text-sm font-bold` |\n| Background | `bg-white bg-opacity-10` |\n| Border | `border border-white border-opacity-20` |\n| Rounded | `rounded-3xl` |\n| Backdrop | `backdrop-blur-[10px]` |\n| Shadow | `shadow-2xl` |\n| Gradient | `bg-gradient-to-r from-blue-400 to-purple-600` |\n| Animation | `animate-fade-in`, `animate-slide-in` |\n| Hover | `hover:bg-opacity-20` |\n| Responsive | `md:col-span-2 lg:col-span-3` |\n\n## 🐛 Debugging Tips\n\n### Check Browser Console\n- Open DevTools: `F12` or `Ctrl+Shift+I`\n- Console tab shows any React errors\n- Network tab shows asset load times\n\n### Inspect Styles\n- Right-click element → \"Inspect\"\n- Check calculated styles\n- Verify Tailwind classes are applied\n\n### Performance Monitor\n- DevTools → Performance tab\n- Record and analyze frame rate\n- Animations should maintain 60fps\n\n### Common Issues\n\n**Issue**: Styles not applying\n**Solution**:\n```bash\nnpm run build          # Rebuild CSS\nCtrl+Shift+Delete      # Clear browser cache\n```\n\n**Issue**: Animation stuttering\n**Solution**:\n- Check for JavaScript during animation\n- Reduce simultaneous animations\n- Check GPU settings in browser\n\n**Issue**: Icons not showing\n**Solution**:\n```typescript\nimport { IconName } from 'lucide-react';\n// Ensure IconName is a valid Lucide icon\n```\n\n## 📚 File Editing Map\n\n| Task | File | Lines |\n|------|------|-------|\n| Add metric | `HealthDashboard.tsx` | 13-20 |\n| Edit Oura data | `OuraMetricsCard.tsx` | 10-24 |\n| Edit Nutrition data | `NutritionCard.tsx` | 14-44 |\n| Change colors | `components/*.tsx` | `color` property |\n| Adjust animations | `tailwind.config.ts` | 28-45 |\n| Edit fonts | `src/index.css` | 5 |\n| Change background | `src/index.css` | 25 |\n\n## 🚀 Next Steps\n\n### Integrate Real Data\n1. Replace dummy data with API calls\n2. Add `useEffect` hook to fetch from Dataverse\n3. Use `useState` to manage loaded data\n\n### Example Dataverse Integration\n```typescript\nimport { useEffect, useState } from 'react';\n\nconst HealthDashboard: React.FC = () => {\n  const [ouraData, setOuraData] = useState([]);\n\n  useEffect(() => {\n    const fetchData = async () => {\n      const response = await fetch('/api/oura-metrics');\n      const data = await response.json();\n      setOuraData(data);\n    };\n    \n    fetchData();\n  }, []);\n\n  return (\n    // Use ouraData instead of hardcoded metrics\n  );\n};\n```\n\n### Deploy to Power Apps\n```bash\n# Once integrated with your Power Platform\npac code push\n```\n\n## 📖 Resources\n\n- [React Documentation](https://react.dev)\n- [Tailwind CSS](https://tailwindcss.com)\n- [Lucide Icons](https://lucide.dev)\n- [Vite Documentation](https://vite.dev)\n- [TypeScript Handbook](https://www.typescriptlang.org/docs/)\n\n## 💬 Code Examples\n\n### Adding a New Card Section\n```typescript\n<GlassCard animated className=\"p-8 col-span-1 lg:col-span-2 animate-slide-in\">\n  <div className=\"flex items-center gap-3 mb-8\">\n    <div className=\"p-3 rounded-full bg-gradient-to-br from-purple-400 to-blue-500 bg-opacity-20\">\n      <YourIcon className=\"w-6 h-6 text-purple-400\" />\n    </div>\n    <h2 className=\"text-2xl font-bold text-white\">Your Card Title</h2>\n  </div>\n  \n  {/* Your content here */}\n</GlassCard>\n```\n\n### Interactive Element\n```typescript\n<button className=\"px-6 py-3 rounded-full backdrop-blur-[10px] bg-white bg-opacity-10 text-white font-semibold transition-all duration-300 hover:bg-opacity-20 active:scale-95\">\n  Click me\n</button>\n```\n\n---\n\n**Need Help?** Check the [HEALTH_APP_README.md](HEALTH_APP_README.md) for architecture details or [ARCHITECTURE.md](ARCHITECTURE.md) for deep dives.\n\n**Version**: 1.0.0  \n**Last Updated**: February 15, 2026\n